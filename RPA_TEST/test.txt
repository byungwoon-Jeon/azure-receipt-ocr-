def _safe_remove(path: str):
    try:
        if path and os.path.exists(path):
            os.remove(path)
            logger.info(f"[정리] 파일 삭제: {path}")
    except Exception as e:
        logger.warning(f"[정리 실패] {path} 삭제 오류: {e}")